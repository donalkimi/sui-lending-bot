import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface SevenKFunProtocolConfig {
    wrappedRouterPackageId: string;
    configurationObjectId: string;
    flowxV2ContainerObjectId: string;
}
export interface SevenKFunSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, SevenKFunProtocolConfig> {
    xForY: boolean;
}
export declare class SevenKFunSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, SevenKFunProtocolConfig, SevenKFunSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    constructor(options: SevenKFunSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
