import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface SuiRewardsMeSwapProtocolConfig {
    wrappedRouterPackageId: string;
    configObjectId: string;
}
export interface SuiRewardsMeSwapSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, SuiRewardsMeSwapProtocolConfig> {
    xForY: boolean;
}
export declare class SuiRewardsMeSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, SuiRewardsMeSwapProtocolConfig, SuiRewardsMeSwapSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    constructor(options: SuiRewardsMeSwapSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
