import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface HopFunSwapConfig {
    wrappedRouterPackageId: string;
    memeConfigObjectId: string;
}
export interface HopFunSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, HopFunSwapConfig> {
    xForY: boolean;
}
export declare class HopFunSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, HopFunSwapConfig, HopFunSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    constructor(options: HopFunSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
