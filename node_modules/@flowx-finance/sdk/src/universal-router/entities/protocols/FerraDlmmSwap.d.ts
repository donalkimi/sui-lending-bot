import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin } from '../../../core';
import { Protocol } from '../../constants';
import { Swap, SwapConstructorOptions } from '../Swap';
export interface DipCoinDexSwapProtocolConfig {
    wrappedRouterPackageId: string;
    globalConfigObjectId: string;
}
export interface FerraDlmmSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, DipCoinDexSwapProtocolConfig> {
    xForY: boolean;
}
export declare class FerraDlmmSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, DipCoinDexSwapProtocolConfig, FerraDlmmSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    readonly swapTimestampMs: number;
    readonly signatures: string[];
    constructor(options: FerraDlmmSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult) => (tx: Transaction) => void;
}
