import { Transaction } from '@mysten/sui/transactions';
import { Coin } from '../../core';
import { BuildTransactionOptions, Trade } from './Trade';
export declare class MultipleTrades<CInput extends Coin, COutput extends Coin> {
    private readonly trades;
    constructor(trades: Trade<CInput, COutput>[]);
    buildTransaction(sender: string, params: BuildTransactionOptions): Promise<Transaction>;
}
