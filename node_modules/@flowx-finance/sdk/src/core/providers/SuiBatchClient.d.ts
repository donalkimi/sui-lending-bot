import { GetObjectParams, SuiClient, SuiClientOptions, SuiObjectResponse } from '@mysten/sui/client';
type SuiBatchClientOptions = {
    scheduleTimeMs: number;
};
export declare class SuiBatchClient extends SuiClient {
    #private;
    scheduleTimeMs: number;
    constructor(options: SuiBatchClientOptions & SuiClientOptions);
    getObject(input: GetObjectParams): Promise<SuiObjectResponse>;
}
export {};
