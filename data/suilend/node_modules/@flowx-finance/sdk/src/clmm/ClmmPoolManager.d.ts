import { NETWORK, TxBuilder } from '../core';
import { ClmmPool } from './entities';
export declare class ClmmPoolManager extends TxBuilder {
    readonly network: NETWORK;
    constructor(network?: NETWORK);
    createPool(pool: ClmmPool): void;
    createPoolV2(pool: ClmmPool): Promise<void>;
    createPoolV3(pool: ClmmPool): void;
    getPoolDetail(poolId: string): Promise<ClmmPool>;
    getPoolDetails(poolIds: string[]): Promise<ClmmPool[]>;
    getPools(): Promise<ClmmPool[]>;
}
