import { DynamicFieldInfo, SuiClient, SuiEvent, SuiEventFilter, SuiObjectData, SuiObjectDataOptions } from '@mysten/sui/client';
import { Coin } from '../entities';
export declare const getAllOwnedObjects: ({ owner, objectType }: {
    owner: string;
    objectType: string;
}, client: SuiClient) => Promise<SuiObjectData[]>;
export declare const getAllDynamicFields: (parentId: string, client: SuiClient) => Promise<DynamicFieldInfo[]>;
export declare const multiGetObjects: (client: SuiClient, objectIds: string[], options?: SuiObjectDataOptions) => Promise<SuiObjectData[]>;
export declare const queryAllEvents: (query: SuiEventFilter, client: SuiClient) => Promise<SuiEvent[]>;
export declare const fetchCoin: (coinType: string) => (client: SuiClient) => Promise<Coin>;
