interface PromiseResolvedResult<T> {
    status: 'resolved';
    data: T;
}
interface PromiseRejectedResult {
    status: 'rejected';
    error: any;
}
type PromiseResult<T> = PromiseResolvedResult<T> | PromiseRejectedResult;
export declare function reflect<T>(promise: Promise<T>): Promise<PromiseResult<T>>;
export declare function PromiseAll<T>(values: Promise<T>[], concurrency?: number): Promise<T[]>;
export {};
