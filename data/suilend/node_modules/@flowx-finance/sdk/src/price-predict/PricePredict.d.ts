import { TransactionArgument } from '@mysten/sui/transactions';
import { NETWORK, TxBuilder } from '../core';
import { IPricePredictPool } from './IPricePredictPool';
export declare class PricePredict extends TxBuilder {
    private readonly packageId;
    private readonly modulePool;
    constructor(network: NETWORK);
    bet(pool: IPricePredictPool, predictedPrice: number | string, coin_in: {
        type: string;
        object: string | TransactionArgument;
    }): import("@mysten/sui/transactions").Transaction;
    claim(pool: IPricePredictPool, betIndex: number): import("@mysten/sui/transactions").Transaction;
}
