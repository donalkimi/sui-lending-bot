import { ExchangeAdapter } from './ExchangeAdapter';
import { Exchange, NETWORK } from '../types';
export declare class ExchangeFactory {
    static createAll(network: NETWORK, includeExchanges?: Exchange[]): ExchangeAdapter[];
}
