import { ExchangeAdapter, NormalizedQuote, QuoteResult } from './ExchangeAdapter';
import { QuoteResponse } from '@bluefin-exchange/bluefin7k-aggregator-sdk';
import { Transaction, TransactionObjectArgument } from '@mysten/sui/transactions';
import { Exchange, QuoteQueryParams } from '../types';
export declare class BluefinAdapter implements ExchangeAdapter<QuoteResponse> {
    name(): Exchange;
    private toNormalized;
    getRoutes(params: QuoteQueryParams): Promise<NormalizedQuote<QuoteResponse> | null>;
    buildTx(tx: Transaction, quote: QuoteResult<QuoteResponse>, sender: string, slippage: number, coinIn: TransactionObjectArgument): Promise<{
        coinOut: any;
        tx: Transaction;
    }>;
}
