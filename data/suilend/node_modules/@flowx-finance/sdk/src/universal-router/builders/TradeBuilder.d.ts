import { BigintIsh, Coin, NETWORK } from '../../core';
import { Commission } from '../entities/Commission';
import { Route } from '../entities/Route';
import { Trade } from '../entities/Trade';
export declare class TradeBuilder<CInput extends Coin, COutput extends Coin> {
    private _network;
    private _sender;
    private _recipient;
    private _amountIn;
    private _amountOut;
    private _slippage;
    private _deadline;
    private _routes;
    private _commission;
    static fromRoutes<CInput extends Coin, COutput extends Coin>(routes: Route<CInput, COutput>[]): TradeBuilder<CInput, COutput>;
    constructor(network: NETWORK, routes: Route<CInput, COutput>[]);
    sender(sender: string): TradeBuilder<CInput, COutput>;
    recipient(recipient: string): TradeBuilder<CInput, COutput>;
    /**
     * @deprecated remove in feature
     * We don't need pass amount in anymore, it always auto calculate
     * */
    amountIn(amountIn: BigintIsh): TradeBuilder<CInput, COutput>;
    /**
     * @deprecated remove in feature
     * We don't need pass amount out anymore, it always auto calculate
     * */
    amountOut(amountOut: BigintIsh): TradeBuilder<CInput, COutput>;
    deadline(deadline: number): TradeBuilder<CInput, COutput>;
    slippage(slippage: number): TradeBuilder<CInput, COutput>;
    commission(commission: Commission): TradeBuilder<CInput, COutput>;
    build(): Trade<CInput, COutput>;
}
