import { Coin, NETWORK } from '../../core';
import { Commission } from '../entities/Commission';
import { Route } from '../entities/Route';
import { MultipleTrades } from '../entities/MultipleTrades';
export declare class MultiTradesBuilder<CInput extends Coin, COutput extends Coin> {
    private _network;
    private _sender;
    private _recipient;
    private _slippage;
    private _deadline;
    private _commissions;
    private _routeGroups;
    static fromRoutesGroups<CInput extends Coin, COutput extends Coin>(routeGroups: Route<CInput, COutput>[][]): MultiTradesBuilder<CInput, COutput>;
    constructor(network: NETWORK, routeGroups: Route<CInput, COutput>[][]);
    sender(sender: string): MultiTradesBuilder<CInput, COutput>;
    recipient(recipient: string): MultiTradesBuilder<CInput, COutput>;
    slippage(slippage: number): MultiTradesBuilder<CInput, COutput>;
    deadline(deadline: number): MultiTradesBuilder<CInput, COutput>;
    commissions(commissions: Commission[]): MultiTradesBuilder<CInput, COutput>;
    build(): MultipleTrades<CInput, COutput>;
}
