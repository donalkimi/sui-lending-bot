import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface AftermathLsdProtocolConfig {
    wrappedRouterPackageId: string;
    afSuiSafeTreasuryCapObjectId: string;
    referralVaultObjectId: string;
    treasuryObjectId: string;
    validatorAddr: string;
}
export interface AftermathLsdSwapSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, AftermathLsdProtocolConfig> {
    xForY: boolean;
}
export declare class AftermathLsdSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, AftermathLsdProtocolConfig, AftermathLsdSwapSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    constructor(options: AftermathLsdSwapSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
