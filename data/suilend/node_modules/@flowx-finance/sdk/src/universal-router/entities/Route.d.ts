import { TransactionResult, Transaction } from '@mysten/sui/transactions';
import { BigintIsh, Coin, NETWORK, Percent } from '../../core';
import { Swap } from './Swap';
export declare class Route<CInput extends Coin, COutput extends Coin> {
    readonly network: NETWORK;
    readonly paths: Swap<CInput, COutput, any, any>[];
    readonly input: CInput;
    readonly output: COutput;
    readonly amountIn: BigintIsh;
    readonly amountOut: BigintIsh;
    readonly slippage: Percent;
    constructor(network: NETWORK, paths: Swap<CInput, COutput, any, any>[]);
    swap: (tradeObject: TransactionResult, slippage: Percent, pythMap: Record<string, string>) => (tx: Transaction) => TransactionResult;
}
