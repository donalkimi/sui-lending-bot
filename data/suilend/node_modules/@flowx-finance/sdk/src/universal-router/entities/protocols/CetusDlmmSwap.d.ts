import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin } from '../../../core';
import { Protocol } from '../../constants';
import { Swap, SwapConstructorOptions } from '../Swap';
export interface CetusDlmmSwapProtocolConfig {
    wrappedRouterPackageId: string;
    globalConfigObjectId: string;
    versionedObjectId: string;
}
export interface CetusDlmmSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, CetusDlmmSwapProtocolConfig> {
    xForY: boolean;
}
export declare class CetusDlmmSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, CetusDlmmSwapProtocolConfig, CetusDlmmSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    readonly swapTimestampMs: number;
    readonly signatures: string[];
    constructor(options: CetusDlmmSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult) => (tx: Transaction) => void;
}
