import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface AftermathProtocolConfig {
    wrappedRouterPackageId: string;
    poolRegistryObjectId: string;
    protocolFeeVaultObjectId: string;
    treasuryObjectId: string;
    insuranceFundObjectId: string;
    referralVaultOjectId: string;
}
export interface AftermathSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, AftermathProtocolConfig> {
    lpCoinType: string;
}
export declare class AftermathSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, AftermathProtocolConfig, AftermathSwapOptions<CInput, COutput>> {
    readonly lpCoinType: string;
    constructor(options: AftermathSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
