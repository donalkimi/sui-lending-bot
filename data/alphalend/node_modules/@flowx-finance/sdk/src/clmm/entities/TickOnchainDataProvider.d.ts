import { Tick } from './Tick';
import { TickDataProvider } from './TickDataProvider';
import { NETWORK } from '../../core';
interface TickOnchainDataProviderConstructorArgs {
    network: NETWORK;
    tickManagerId: string;
}
/**
 * A data provider for ticks that is backed by by onchain queries.
 */
export declare class TickOnchainDataProvider implements TickDataProvider {
    readonly network: NETWORK;
    private readonly tickManagerId;
    private readonly client;
    constructor({ network, tickManagerId, }: TickOnchainDataProviderConstructorArgs);
    getTick(index: number): Promise<Tick>;
}
export {};
