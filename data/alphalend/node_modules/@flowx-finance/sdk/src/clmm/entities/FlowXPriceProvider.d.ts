import { NETWORK } from '../../core';
import { PriceProvider } from './PriceProvider';
export declare class FlowXPriceProvider implements PriceProvider {
    #private;
    constructor(network: NETWORK);
    getPrice(token: string): Promise<number>;
}
