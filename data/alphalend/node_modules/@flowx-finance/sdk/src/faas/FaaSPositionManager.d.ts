import { NETWORK, TxBuilder } from '../core';
import { FaaSPosition } from './entities';
export declare class FaaSPositionManager extends TxBuilder {
    network: NETWORK;
    private readonly poolManager;
    constructor(network?: NETWORK);
    getUserPositions(owner: string): Promise<FaaSPosition[]>;
}
