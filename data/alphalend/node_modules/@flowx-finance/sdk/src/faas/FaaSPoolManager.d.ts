import { NETWORK, TxBuilder } from '../core';
import { FaaSPool } from './entities';
export declare class FaaSPoolManager extends TxBuilder {
    private readonly liquidityPoolManager;
    constructor(network: NETWORK);
    private getLiquidityPool;
    private multiGetLiquidityPools;
    getPool(poolIndex: number): Promise<FaaSPool>;
    multiGetPools(poolIndexes: number[]): Promise<FaaSPool[]>;
    getAllPools(): Promise<FaaSPool[]>;
}
