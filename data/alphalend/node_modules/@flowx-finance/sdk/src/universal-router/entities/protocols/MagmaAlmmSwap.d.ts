import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin } from '../../../core';
import { Protocol } from '../../constants';
import { Swap, SwapConstructorOptions } from '../Swap';
export interface DipCoinDexSwapProtocolConfig {
    wrappedRouterPackageId: string;
    globalConfigObjectId: string;
    factoryConfigObjectId: string;
}
export interface MagmaAlmmSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, DipCoinDexSwapProtocolConfig> {
    xForY: boolean;
}
export declare class MagmaAlmmSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, DipCoinDexSwapProtocolConfig, MagmaAlmmSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    readonly swapTimestampMs: number;
    readonly signatures: string[];
    constructor(options: MagmaAlmmSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult) => (tx: Transaction) => void;
}
