import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface DipCoinDexSwapProtocolConfig {
    wrappedRouterPackageId: string;
    globalObjectId: string;
}
export interface DipCoinDexSwapSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, DipCoinDexSwapProtocolConfig> {
    xForY: boolean;
}
export declare class DipCoinDexSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, DipCoinDexSwapProtocolConfig, DipCoinDexSwapSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    constructor(options: DipCoinDexSwapSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
