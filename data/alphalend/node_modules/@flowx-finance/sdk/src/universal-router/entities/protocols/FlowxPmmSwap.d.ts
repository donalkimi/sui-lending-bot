import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface FlowxPmmProtocolConfig {
    sourcePackageId: string;
    wrappedRouterPackageId: string;
    stateObjectId: string;
}
interface FlowxPmmSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, FlowxPmmProtocolConfig> {
    xForY: boolean;
    swapTimestampMs: number;
    signatures: string[];
}
export declare class FlowxPmmSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, FlowxPmmProtocolConfig, FlowxPmmSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    readonly swapTimestampMs: number;
    readonly signatures: string[];
    constructor(options: FlowxPmmSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
export {};
