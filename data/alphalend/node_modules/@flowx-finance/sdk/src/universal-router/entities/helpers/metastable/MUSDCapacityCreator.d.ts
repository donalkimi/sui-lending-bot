import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { IMetaCapacityCreator } from './IMetaCapicityCreator';
export declare class MUSDCapacityCreator implements IMetaCapacityCreator {
    createDepositCap: (coinIn: string, config: any) => (tx: Transaction) => TransactionResult;
    createWithdrawCap: (coinOut: string, config: any) => (tx: Transaction) => TransactionResult;
}
