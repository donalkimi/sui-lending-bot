import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions, WrappedRouterConfig } from '../Swap';
import { Protocol } from '../../constants';
import { OracleInfo } from '../../types';
export interface SevenKV1DexProtocolConfig extends WrappedRouterConfig {
    oraclePackageId: string;
}
export interface SevenKV1DexSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, SevenKV1DexProtocolConfig> {
    xForY: boolean;
    oracles: OracleInfo[];
    poolStructTag: string;
}
export declare class SevenKV1DexSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, SevenKV1DexProtocolConfig, SevenKV1DexSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    readonly poolStructTag: string;
    constructor(options: SevenKV1DexSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, _: Percent, pythMap: Record<string, string>) => (tx: Transaction) => void;
}
