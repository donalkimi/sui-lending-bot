import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { BigintIsh, Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface DeepbookV3ProtocolConfig {
    wrappedRouterPackageId: string;
    treasuryObjectId: string;
}
export interface DeepbookV3SwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, DeepbookV3ProtocolConfig> {
    xForY: boolean;
    deepFeeAmount: BigintIsh;
}
export declare class DeepbookV3Swap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, DeepbookV3ProtocolConfig, DeepbookV3SwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    readonly deepFeeAmount: BigintIsh;
    constructor(options: DeepbookV3SwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
