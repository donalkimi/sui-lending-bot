import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface BlueMoveFunProtocolConfig {
    wrappedRouterPackageId: string;
    configurationObjectId: string;
    thresholdObjectId: string;
    dexInfoObjectId: string;
}
export interface BlueMoveFunSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, BlueMoveFunProtocolConfig> {
    xForY: boolean;
}
export declare class BlueMoveFunSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, BlueMoveFunProtocolConfig, BlueMoveFunSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    constructor(options: BlueMoveFunSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
