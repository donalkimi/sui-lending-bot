import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface VoloLsdProtocolConfig {
    wrappedRouterPackageId: string;
    vSuiMetadataObjectId: string;
}
export interface VoloLsdSwapSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, VoloLsdProtocolConfig> {
    xForY: boolean;
}
export declare class VoloLsdSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, VoloLsdProtocolConfig, VoloLsdSwapSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    constructor(options: VoloLsdSwapSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
