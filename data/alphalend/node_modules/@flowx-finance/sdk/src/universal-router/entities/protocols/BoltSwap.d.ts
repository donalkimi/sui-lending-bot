import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin } from '../../../core';
import { Protocol } from '../../constants';
import { Swap, SwapConstructorOptions } from '../Swap';
export interface BoltProtocolConfig {
    wrappedRouterPackageId: string;
}
export interface BoltSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, BoltProtocolConfig> {
    xForY: boolean;
}
export declare class BoltSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, BoltProtocolConfig, BoltSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    constructor(options: BoltSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult) => (tx: Transaction) => void;
}
