import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Protocol } from '../../constants';
import { Swap, SwapConstructorOptions, WrappedRouterConfig } from '../Swap';
export interface IpxTideSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, WrappedRouterConfig> {
}
export declare class IpxTideSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, WrappedRouterConfig, IpxTideSwapOptions<CInput, COutput>> {
    constructor(options: IpxTideSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent, pythMap: Record<string, string>) => (tx: Transaction) => void;
}
