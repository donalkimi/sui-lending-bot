import { Transaction, TransactionResult } from '@mysten/sui/transactions';
import { Coin, Percent } from '../../../core';
import { Swap, SwapConstructorOptions } from '../Swap';
import { Protocol } from '../../constants';
export interface TurbosFunProtocolConfig {
    wrappedRouterPackageId: string;
    configurationObjectId: string;
}
export interface TurbosFunSwapOptions<CInput extends Coin, COutput extends Coin> extends SwapConstructorOptions<CInput, COutput, TurbosFunProtocolConfig> {
    xForY: boolean;
}
export declare class TurbosFunSwap<CInput extends Coin, COutput extends Coin> extends Swap<CInput, COutput, TurbosFunProtocolConfig, TurbosFunSwapOptions<CInput, COutput>> {
    readonly xForY: boolean;
    constructor(options: TurbosFunSwapOptions<CInput, COutput>);
    protocol(): Protocol;
    swap: (routeObject: TransactionResult, slippage: Percent) => (tx: Transaction) => void;
}
